## Testing

The project includes unit and integration tests.

Unit tests cover GUI behaviour, record validation, repository operations, searching, service logic, and shared pagination utilities, with dependencies isolated so components can be tested independently of the full application.

Integration tests verify record workflows, save-failure consistency, and search pagination across connected modules.

The end-to-end structure supports future workflow testing.
